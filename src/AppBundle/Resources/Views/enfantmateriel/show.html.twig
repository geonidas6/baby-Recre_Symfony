{% extends '@App/user_base.html.twig' %}

    {% block title %} {{ parent() }} Bienvenue {% endblock %}
  {% block main %}
    <h1>Enfantmateriel</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ enfantMateriel.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ enfantMateriel.quantite }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('enfantmateriel_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('enfantmateriel_edit', { 'id': enfantMateriel.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
